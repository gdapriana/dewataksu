datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

enum Role {
  USER
  ADMIN
}

enum ActivityType {
  CREATE
  UPDATE
  DELETE
}

enum Schema {
  CATEGORY
  DESTINATION
  DISTRICT
  STORY
  TRADITION
  USER
}
